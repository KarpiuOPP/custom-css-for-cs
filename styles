$background: rgba(18, 18, 18, 0.85); // 0.85 to 85% opacity
$border-1: rgba(89, 89, 89, 0.85);
$border-2: rgba(141, 141, 141, 0.85);

.dark-theme {

    * {
        padding: 0;
        margin: 0;
        font-family: 'DM Sans', sans-serif;
        color: white;
    }

    .wrapper {
        display: flex;
        flex-direction: column;
        justify-content: center;
        background: linear-gradient($border-rotation, $border-1, $border-2);
        border-radius: 12px;
        padding: 1px;

        .widget {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 12px;

            .level {
                display: flex;
                align-items: center;

                img {
                    $size: 70px;
                    width: $size;
                    height: $size;
                    margin-right: 12px;
                }

                .elo {
                    p {
                        font-size: .85em;
                    }
                }
            }

            .matches {
                text-align: center;
                small {
                    font-size: .6em;
                }
                .stats {
                    display: flex;
                }
            }

            border-radius: 12px;
            background: $background;
        }

        .stat {
            display: flex;
            flex-direction: column;
            border: 1px solid #fff;
            $size: 54px;
            width: $size;
            height: $size;
            align-items: center;
            justify-content: center;
            font-size: calc($size - 37px);
            border-radius: 12px;
            margin-left: 6px;

            p {
                font-weight: bold;
            }
        }

        .stat.green {
            border: 1px solid rgba(0, 255, 95, 0.85);
            background: rgba(17, 49, 24, 0.85);
        }

        .stat.red {
            border: 1px solid rgba(255, 34, 34, 0.85);
            background: rgba(54, 14, 14, 0.85);
            color: rgba(255, 34, 34, 0.85);
        }
    }
}
